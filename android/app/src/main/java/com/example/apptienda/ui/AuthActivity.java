/**
package com.example.apptienda.ui;

/**
 * AuthActivity (UI stub)
 * - Stub para la pantalla de autenticación. La lógica de autenticación se mantiene en
 *   `ServicioAuth` y `AuthManager`. Si el equipo de frontend necesita una UI, pueden
 *   implementar su propia pantalla y usar esos servicios.
 */
public class AuthActivity {

	// Clase placeholder: no hay implementación UI en esta rama.

}
